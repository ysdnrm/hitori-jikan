<div class="container  mt-5 pb-5">
  <div class="col-md-12">
    <h2 class="mb-3 ml-5 text-center">タグが「<%= @search_shop_tag.name %>」の投稿一覧</h2>
    <% if @shop_tag_post_results.present? %>
      <div class="row">
        <%= render 'post', posts: @shop_tag_post_results %>
      </div>
    <% else %>
      <p>一致する情報は見つかりませんでした</p>
    <% end %>
  </div>
</div>