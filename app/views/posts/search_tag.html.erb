<div class="container  mt-5 pb-5">
  <div class="col-md-12">
    <h2 class="mb-3 ml-5 text-center font-weight-bold">タグが「<%= @search_shop_tag.name %>」の投稿一覧</h2>
    <p class="mb-3 ml-5 text-center font-weight-bold">
      <i class="fas fa-exclamation-triangle"></i>
      非公開中の投稿は表示されません
    </p>
    <% if @shop_tag_post_results.present? %>
      <div class="row">
        <%= render 'post', posts: @shop_tag_post_results %>
      </div>
    <% else %>
      <p class="mt-5 ml-5 text-center">一致する情報は見つかりませんでした</p>
    <% end %>
  </div>
</div>